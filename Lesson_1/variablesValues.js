'use strict';

let printNumbers = function(numbers) {
    numbers = [1, 2, 3, 4, 5];
    console.log(numbers);
    }

    
let numbers = [1, 2, 3, 4, 5, 6, 7, 8];
console.log(numbers);
printNumbers(numbers);
